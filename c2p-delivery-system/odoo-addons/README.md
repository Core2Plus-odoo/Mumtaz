# Odoo addons

Custom Odoo modules, organized per Odoo.sh project (add each project's addons repo
as a git submodule or subfolder). Modules generated by the Developer agent land in
the relevant project's repo and deploy via Odoo.sh.
